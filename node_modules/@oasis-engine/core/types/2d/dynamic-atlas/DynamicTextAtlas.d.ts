/// <reference types="offscreencanvas" />
import { Engine } from "../../Engine";
import { Sprite } from "../sprite/Sprite";
/**
 * Dynamic atlas for text.
 */
export declare class DynamicTextAtlas {
    private static _region;
    private _texture;
    private _width;
    private _height;
    private _space;
    private _curX;
    private _curY;
    private _nextY;
    private _sprites;
    constructor(engine: Engine, width: number, height: number);
    /**
     * Destroy atlas, it will release the texture.
     */
    destroy(): void;
    /**
     * Add a sprite.
     * @param sprite - the sprite to add
     * @param imageSource - The source of texture
     * @returns true if add sprite success, otherwise false
     */
    addSprite(sprite: Sprite, imageSource: TexImageSource | OffscreenCanvas): boolean;
    /**
     * Remove a sprite.
     * @param sprite - the sprite to remove
     * @returns true if remove sprite success, otherwise false
     */
    removeSprite(sprite: Sprite): boolean;
}
